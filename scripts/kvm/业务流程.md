

1，系统创建一个namespace router-0，并创建一对veth pair，
veth的一端ext-vlan 连接到br-vlan，br-vlan绑定变量vlan-interface，
这个veth 的另一端link-vlan绑定到router-0,并配置公网ip地址，设置默认路由，nat规则，源ip转换规则，把所有vpc的流量通过 router0到br-vlan里的vlan接口发送出去，

public gateway <----- （ vlan-interface，物理接口）* br-vlan（桥） * ext-vlan  <--> link-vlan（公网ip） * router-0（namespace  默认路由指向public gateway） 




列出主要步骤，忽略脚本初始化与环境加载与参数校验与赋值部分，使用列表列出来 