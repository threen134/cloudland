{{template "_head" .}}
<div class="admin user">

    <div class="ui container">

        <div class="ui grid">

        {{template "_left" .}}
			<div class="user signup">
				<div class="ui middle very relaxed page grid">
					<div class="" >
						<form class="ui form" action="{{.Link}}" method="post">
							<h3 class="ui top attached header">
								{{.i18n.Tr "Create New Listener"}}
							</h3>
							<div class="ui attached segment">
								<div class="required inline field">
									<label for="name">{{.i18n.Tr "Name"}}</label>
									<input id="name" name="name" autofocus required>
								</div>
								<div class="required inline field">
									<label for="mode">{{.i18n.Tr "Mode"}}</label>
									<div class="ui selection dropdown">
									  <input id="mode" name="mode" type="hidden">
									  <i class="dropdown icon"></i>
									  <div class="default text">{{.i18n.Tr "http"}}</div>
									  <div class="menu">
										<div class="item" data-value="http" data-text="http">{{.i18n.Tr "http"}}</div>
										<div class="item" data-value="tcp" data-text="tcp">{{.i18n.Tr "tcp"}}</div>
									  </div>
									</div>
								</div>
                                                                <div class="required inline field">
                                                                        <label for="port">{{.i18n.Tr "Port"}}</label>
                                                                        <input id="port" name="port">
                                                                </div>
								<div class="inline field">
									<label>{{.i18n.Tr "Key"}}</label>
									<textarea id="key"  name="key">{{.Key}} </textarea>
								</div>
								<div class="inline field">
									<label>{{.i18n.Tr "Certificate"}}</label>
									<textarea id="cert"  name="cert">{{.Certificate}} </textarea>
								</div>
								<div class="inline field">
									<label></label>
									<button class="ui green button">{{.i18n.Tr "Create New Listener"}}</button>
								</div>
							</div>
						</form>
					</div>
				</div>
			</div>
		</div>
	</div>
</div>
{{template "_footer" .}}

