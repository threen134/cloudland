# 存储卷指南

## 概述

CloudLand 的存储服务提供持久化的块存储能力。

### 存储特性

- **按需扩展**：支持在线扩容 (Online Resize)。
- **持久化隔离**：存储卷独立于虚拟机实例的生命周期。
- **动态挂载**：支持热插拔 (Hotplug)。
- **备份快照**：支持秒级快照创建与数据回滚。

## 性能优化

- **NVMe 极致速度**：通过底层 SCI 框架直通 NVMe 资源，降低 IO 延迟。
- **QoS 限制**：支持对不同存储卷设置 IOPS 和带宽上限。
